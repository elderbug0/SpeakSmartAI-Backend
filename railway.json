{
    "$schema": "https://railway.app/railway.schema.json",
    "deploy": {
        "startCommand": "python app.py migrate && python app.py collectstatic --noinput && gunicorn hashfire_api.asgi:application"
    }
}